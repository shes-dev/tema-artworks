version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: tema-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped

volumes:
  mongo-data:
